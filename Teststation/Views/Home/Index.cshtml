@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "Startseite";
}

<h2>Hallo @UserManager.GetUserName(User)!</h2>